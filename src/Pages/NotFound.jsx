import React from 'react';

/**
 * For preventing unwanted redirection in site, i just handle it with 404-No Page Found 
 */

const NotFound = props => {
    return (
        <React.Fragment>
            Page Not Found.
        </React.Fragment>
    );
};

export default NotFound;